# SQL 문


```sql

영화 삽입

- 일반 영화 테이블에 영화 삽입
insert into video(title, description, view_count, release_date, video_img_url) values (DATA);
- 영화 detail 테이블에 삽입
insert into video_detail(video_id, video_url, sequence) values (DATA);

- 유저 테이블에 유저 삽입
insert into user_table(user_id, user_
```